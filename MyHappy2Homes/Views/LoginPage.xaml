<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyHappy2Homes.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <StackLayout>
                <Label VerticalOptions="Center" Text="Email"></Label>
            <Entry VerticalOptions="Center" Text="{Binding UserEmail, Mode=TwoWay}" Placeholder="Enter Email" x:Name="emailEntry">
            </Entry>
                <Label VerticalOptions="Center" Text="Password"></Label>
                <Entry VerticalOptions="Center" Text="{Binding Password, Mode=TwoWay}" Placeholder="Enter Password" x:Name="passwordEntry" IsPassword="True">
            </Entry>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
                <Label VerticalOptions="Center" Text="Dont have an account? signup now" TextColor="Gray" x:Name="signupLabel"></Label>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>